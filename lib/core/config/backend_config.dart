/// Konfigurasi mode backend
class BackendConfig {
  /// Set ke true untuk menggunakan API Backend
  /// Set ke false untuk menggunakan Supabase langsung
  /// TEMPORARY: Set ke false untuk menghindari error
  static const bool useApi = false;
}
